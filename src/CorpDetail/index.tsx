import React from 'react';
import Chart from '../Chart';
import Styled from './styles';

const CorpDetail = () => {
  return (
    <Styled.Root>
      <Chart />
    </Styled.Root>
  );
};

export default CorpDetail;
